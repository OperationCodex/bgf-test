
# Lines in this file are patterns BFG will replace with ***REMOVED*** by default.
# See: bfg --replace-text replacements.txt

# Replace any value that looks like a Stripe secret key
regex:sk_(?:live|test)_[A-Za-z0-9_\-]{10,}

# Replace GitHub classic tokens starting with ghp_
regex:ghp_[A-Za-z0-9]{20,}

# Replace AWS access key IDs (simplified pattern) and corresponding secret keys
regex:AKIA[0-9A-Z]{16}
regex:(?i)aws_secret_access_key\s+[A-Za-z0-9/+]{30,}={0,2}

# Replace emails (very simplified)
regex:[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}

# Replace our specific SECRET_KEY env var values
SECRET_KEY=
STRIPE_SECRET=

# Replace Slack webhook URLs
regex:https://hooks\.slack\.com/services/[A-Za-z0-9/]+ 
